name: Build Radiofm

on:
  push:
    branches: main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true
          components: rustfmt, clippy

      - name: Cache Rust Dependencies
        run: cargo build --release --features hardware

      - name: Run Tests
        run: cargo test --features hardware

      - name: Upload Release Binary
        uses: actions/upload-artifact@v4
        with:
          name: cdrip-release
          path: target/release/cdrip*
          retention-days: 7
